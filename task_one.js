 document.write(`Задача 1`); 
     const array = [
            'каждый',
            'охотник',
            'желает',
            'знать',
            'где',
            'сидит',
            'фазан' 
                   ];

        for (let i = array.length - 1; i >= 0; i--)
        {
 document.write(`<div class = "red">${array[i]}</div>`);
        }
     
    
    